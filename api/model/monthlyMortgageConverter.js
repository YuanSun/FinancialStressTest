var calculator = require('../utilities/financialCalculator');
module.exports = function(totalMortgage) {
    var rate = 0.0325;
    var nPer = 30 * 12;
    
    var monthlyMortgage = calculator.PV(0.00)

    return arr;
}